// 게임 전역 설정 파일
// ⚠️ 로직 금지, 값만 정의

export const Config = {
  /* ===============================
   * 화면 / 해상도
   * =============================== */
  screen: {
    width: window.innerWidth,
    height: window.innerHeight,
    backgroundColor: 0x020b18
  },

  /* ===============================
   * 플레이어 (고래상어)
   * =============================== */
  shark: {
    baseSpeed: 0.6,          // 기본 유영 속도
    acceleration: 0.02,     // 클릭 시 가속
    maxSpeed: 1.2,
    size: 1.0               // 스케일 기준값
  },

  /* ===============================
   * 수집 / 동료
   * =============================== */
  collection: {
    radius: 60,             // 수집 판정 거리
    maxFollowers: 20        // 화면에 허용되는 최대 동료 수
  },

  /* ===============================
   * 비주얼 성장
   * =============================== */
  visualGrowth: {
    starPatternStep: 3,     // 별무늬 증가 주기 (n마리당)
    color plotting: true,
    maxStage: 5
  },

  /* ===============================
   * 파티클
   * =============================== */
  particles: {
    trailInterval: 120,     // ms 단위
    glowIntensity: 0.6
  },

  /* ===============================
   * 바다 / 분위기
   * =============================== */
  ocean: {
    driftSpeed: 0.1,        // 배경 흐름 속도
    depthLayers: 3          // 2.5D 레이어 수
  },

  /* ===============================
   * 색상 팔레트
   * =============================== */
  colors: {
    ocean: 0x020b18,
    sharkBase: 0x4da6ff,
    friend: 0xffee88,
    jellyfishGlow: 0x88ccff
  },

  /* ===============================
   * 디버그
   * =============================== */
  debug: {
    showHitbox: false
  }
};

